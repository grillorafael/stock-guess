.container
    .row
        .col-lg-6.col-lg-offset-3.text-center
            legend Stock Guess
    div(ng-if='!allSent')
        .row
            .col-lg-6.col-lg-offset-3
                h3.text-center(ng-if='isBonus')
                    span.label.label-warning Bonus Round!
                #container(style="height: 200px; min-width: 460px")
        br
        .row(ng-if='!sent')
            .col-lg-6.col-lg-offset-3.text-center
                h4#evaluateDate
                    | Guess the stock for {{ evaluateDate  | date }}
        .row
            .col-lg-6.col-lg-offset-3
                form(ng-submit='send()', novalidate, ng-if='!sent')
                    input(type='text', class='hidden', ng-model='guess.stockName')
                    p(ng-show='error', class='help-block text-center') Please type your guess correctly
                    div(class='input-group', ng-class='{"has-error": error}')
                        input(type='text', ng-model='guess.stockValue', placeholder='Please type your guess', class='form-control', required)
                        span(class="input-group-btn")
                            button(class="btn btn-default", type="submit") Send
                div(ng-if='sent', class='text-center')
                    h4 Thank you for your guess!
                    h4(ng-if='!hasBonus')
                        a(href="", ng-click='loadNextGuess()') Try another stock!
                    h4(ng-if='hasBonus')
                        a(href="", ng-click='loadNextGuess()') BONUS ROUND! Click here to try it!
    .text-center(ng-if='allSent')
        .row
            .col-lg-6.col-lg-offset-3
                h4
                    | You already guessed all of our stock!
                h4
                    | Thanks for collaborating
                h4
                    | Please come back tomorow to try again!
    .row
        .col-lg-6.col-lg-offset-3.text-center
            hr
            p
                small
                    | All the values are in USD
            p
                small
                    | Form submission is private. Your data will only be seen by the people involved in this project.
            p
                small
                    | The purpose of this project is to evaluete the power of &nbsp;
                    a(href='http://en.wikipedia.org/wiki/Collective_intelligence', target='_blank') Collective Intelligence
                    |.
            p
                small
                    | This data is for academic purpose. It will not be used for any personal benefits.
            p
                small
                    | By sending your guess you agree with our &nbsp;
                    a(ng-href='#_=_/terms', target='_blank') terms of use.
